use super::{TCPInfo, TCPItem};

impl TCPInfo {
    pub fn get_items() -> Vec<TCPItem> {
        Vec::new()
    }
    
    pub fn get_pid_port(port : u16) -> Vec<u32> {
        Vec::new()
    }

    
}